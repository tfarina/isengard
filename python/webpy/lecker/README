Bookmarking web service.

http://webpy.org/src/lecker

lec.ker - a simple del.icio.us clone

by Valentin Wüstholz

# To setup MySQL database and user, run the following command line:

$ ./setup_db.sh

# To import the tables, run the following command line and type in hannibal's
# password: 19440807

$ mysql -u hannibal -p lecker < schema.mysql

# How to run it:

$ sudo python ./lecker.py #port-number-you-want-available (preferable 80).
