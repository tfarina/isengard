http://webpy.org/src/todo-list/0.3

# To setup MySQL database and user, run the following command line:

$ ./setup_db.sh

# To import the tables, run the following command line and type in kevin's
# password: chevrolet

$ ./setup_tables.sh

# How to run it:

$ sudo python ./todo.py #port-number-you-want-available (preferable 80).
