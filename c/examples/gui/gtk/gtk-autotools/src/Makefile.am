## Process this file with automake to generate a Makefile.in.

NULL =

bin_PROGRAMS = gat

gat_SOURCES = \
	hello.c \
	$(NULL)

gat_CFLAGS = \
	$(GTK_CFLAGS) \
	$(NULL)

gat_LDADD = \
	$(GTK_LIBS) \
	$(NULL)

-include $(top_srcdir)/git.mk
